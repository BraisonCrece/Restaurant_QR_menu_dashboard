<div class="flex flex-col w-[90%] max-w-2xl h-full">
  <div class="flex justify-center">
    <%= render "shared/add_button", path: new_allergen_path %>
  </div>
  <h1 class="font-thin text-4xl text-center mb-8 text-pretty">Alérxenos</h1>
  <div class="py-6 grid auto-cols-max grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 relative">
    <% @allergens.each do |allergen| %>
        <%= render "allergen", allergen: allergen %>
    <% end %>
  </div>
</div>
