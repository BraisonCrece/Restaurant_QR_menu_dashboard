<!DOCTYPE html>
<html>
  <head>
    <title>Taberna Navegación</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag 'Navega_sinfondo.png' %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300;400;700;900&family=Playfair+Display:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  </head>
  <body class='flex flex-col w-auto h-full justify-center mt-0'>
    <div class="flex justify-center">
      <%= render 'shared/flash' %>
    </div>
    <div data-controller='menu'>
      <% if current_user %>
        <button class="absolute right-5 top-4" data-action="click->menu#toggle"><%= image_tag 'menu.svg', width: 40, heigth: 40, class: 'select-none' %></button>
        <div class="fixed bg-white top-0 right-0 shadow z-20 h-full flex flex-col min-w-[45%] md:min-w-[25%]  ease-in-out duration-300 p-5 translate-x-full " data-menu-target="items">
          <button class="mr-8 mb-3  select-none" data-action="click->menu#toggle"><%= image_tag 'close.svg', width: 40, heigth: 40, class: 'select-none' %></button>
          <%= render '/shared/items' %>
        </div>
      <% end %>
      <div class='flex justify-center p-4 gap-8 mt-5' id='logo-link'>
        <%= link_to root_path do %>
          <%= image_tag 'Navega_sinfondo.png', width: "115px", class: '' %>
        <% end %>
      </div>
    </div>
    <main class="w-full mb-4 flex justify-center">
      <%= yield %>
    </main>
  </body>
</html>
