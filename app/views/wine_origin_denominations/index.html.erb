<div class='w-full max-w-3xl mx-auto p-3'>
  <h1 class="font-thin text-4xl text-center mb-8 text-pretty">Denominacións de Orixe</h1>
  <div class="flex justify-center">
    <%= render "shared/add_button", path: new_denomination_path %>
  </div>
  <div class="py-6 grid auto-cols-max grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 relative">
    <% @denominations.each do |denomination| %>
        <%= render "denomination", denomination: denomination %>
    <% end %>
  </div>
</div>
