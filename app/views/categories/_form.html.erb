<%= form_with(model: category, data: { turbo: false }, class: "shadow rounded-[3px] px-8 pt-6 pb-8 my-4 dark:shadow-white/50") do |f| %>
    <% if @category.errors.any? %>
        <div id="error_explanation" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-[3px] mb-4">
            <h2 class='font-bold mb-2'>
                <%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:
            </h2>
            <ul class="list-disc ml-5">
                <% @category.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <div class="form-group mb-4">
        <%= f.label :name, "Nome", class: "block text-gray-700 font-bold mb-2 dark:text-gray-200" %>
        <%= f.text_field :name, class: "form-input mt-1 block w-full rounded-[3px] border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50 dark:text-gray-800" %>
    </div>
    <div class="form-group mb-4">
        <%= f.label :category_type, "Tipo", class: "block text-gray-700 font-bold mb-2 dark:text-gray-200" %>
        <%= f.select :category_type, [%w[Carta menu], %w[Menú daily]], {}, class: "form-select mt-1 block rounded-[3px] border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50 w-full dark:text-gray-800" %>
    </div>
    <div class="flex items-center justify-between">
        <%= f.submit "Gardar", class: "w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-[3px] focus:outline-none focus:shadow-outline-blue focus:ring ring-blue-300 focus:border-blue-300 active:bg-blue-800" %>
    </div>
<% end %>
