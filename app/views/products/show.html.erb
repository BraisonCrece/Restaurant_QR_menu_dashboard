<div class="max-w-lg mx-auto bg-white rounded-lg overflow-hidden shadow-xl mb-10">
  <div class="min-w-full">
    <%= image_tag @product.picture , class: "w-full rounded-t-lg object-cover" %>
  </div>
  <div class="px-6 py-4">
    <div class="font-bold text-2xl mb-2 playfare"><%= @product.title %></div>
    <p class="text-gray-700 mb-4 lato"><%= @product.description %></p>
    <div class="flex justify-between items-center align-middle">
      <div class="font-semibold text-xl block self-center"><%= number_to_currency(@product.prize, { unit: '€', separator: ',', format: "%n %u" }) %></div>
      <div class="flex alergenos">
        <% @product.allergens.each do |allergen| %>
          <span class="inline-block px-3 py-1 mr-2">
            <%= image_tag allergen.icon, width: "35px" %>
          </span>
        <% end %>
      </div>
    </div>
  </div>
</div>
