<% @categories.each do |category| %>
  <% next if category.products.blank? %>
  <%= link_to translate("category.#{category.id}.name", category.name).gsub(/[\p{Emoji_Presentation}\u{FE0F}]|[\u{1F600}-\u{1F64F}\u{1F37D}]/, ""),
              "#category-#{category.id}",
              class: "text-lg font-light italic align-middle text-center font-medium text-blue-950 subpixel-antialiased hover:text-teal-900 dark:text-emerald-200",
              data: { turbo_frame: "_top", controller: "scroll", action: "click->scroll#scrollToCategory" } %>
<% end %>
<%= link_to translate("category.Viños.name", "Viños").gsub(/[\p{Emoji_Presentation}\u{FE0F}]|[\u{1F600}-\u{1F64F}\u{1F37D}]/, ""), "#viños", class: "text-lg font-light italic align-middle text-center font-medium text-blue-950 subpixel-antialiased hover:text-teal-900 dark:text-emerald-200", data: { turbo_frame: "_top", controller: "scroll", action: "click->scroll#scrollToCategory" } %>
