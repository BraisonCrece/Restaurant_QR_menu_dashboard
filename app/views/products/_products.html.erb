<%= turbo_frame_tag :products, class: 'w-full max-w-lg' do %>
  <% products.each do |product| %>
    <div class='flex justify-between w-full h-[75px] border-t align-middle'>
      <p class='max-w-[75%] self-center flex flex-col gap-2'>
        <%= product.title %>
      </p>
      <p class='self-center'>
        <%= link_to '🔴', toggle_active_path(product_id: product.id), class: "no-underline select-none text-xl", data: { turbo_method: :post } if !product.active%>
        <%= link_to '🟢', toggle_active_path(product_id: product.id), class: "no-underline select-none text-xl", data: { turbo_method: :post } if product.active%>
      </p>
      <div class='fixed flex gap-2'>
        <%= button_to 'Editar', edit_product_path(product), data: { turbo: false }, class: 'text-xs font-thin text-blue-800 p-0', method: :get%>
        <%= button_to 'Eliminar', product_path(product), confirm: 'Seguro que queres eliminar o producto?', data: { turbo: false }, class: 'text-xs font-thin text-red-800 p-0', method: :delete %>
      </div>
    </div>
  <% end %>
<% end %>
